{
  "name": "hr-leave-system-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && rm -rf dist public && mkdir public && cp -rv frontend/dist/* public/"
  }
}
